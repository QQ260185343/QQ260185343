using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class Button_ClickReal : MonoBehaviour
{
    public GameObject PanelInput;
    // 初始化
    void Awake()
    {
        //if (PanelInput==null)
        //{
        //    PanelInput = GameObject.Find("PanelInput");
        //}
       
        //gameObject.GetComponent<Button>().onClick.AddListener(() => { PanelInput.SetActive(true); });

        //Invoke("WaitTofalse",0.3f);
    }
  
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
